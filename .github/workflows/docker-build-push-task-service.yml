name: Build and Push Task Service Docker Image

on:
  push:
    branches:
      - develop
    paths:
      - 'task-service/**'
      - '.github/workflows/docker-build-push-task-service.yml'
  workflow_dispatch:
jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build all modules
        run: mvn clean install

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD_TASK }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./task-service
          file: ./task-service/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/task-service:latest